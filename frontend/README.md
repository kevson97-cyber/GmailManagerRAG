# GmailManagerRAG — frontend

Next.js UI for the local FastAPI backend. See the [root README](../README.md) for full setup, deployment (Vercel + Cloudflare Tunnel), and the security model.

```bash
npm install
npm run dev   # http://localhost:3000 — set backend URL + API token via the gear icon
```
